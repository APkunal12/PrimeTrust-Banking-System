version: '3'

services:

  banking-app:
    build: .
    ports:
      - "80:8000"

  grafana:
    image: grafana/grafana
    ports:
      - "3000:3000"

  jenkins:
    image: jenkins/jenkins:lts
    ports:
      - "8080:8080"